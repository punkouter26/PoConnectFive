# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: PoConnectFive
metadata:
  template: PoConnectFive@0.0.1-beta
services:
  web:
    project: ./PoConnectFive.Server
    language: dotnet
    host: appservice
